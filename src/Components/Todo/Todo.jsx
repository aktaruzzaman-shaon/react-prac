import React, { useState } from 'react';

const Todo = () => {
    const [tasks, setTasks] = useState(initialTasks);

    return (
        <div>
            <h1>Task management</h1>
            
        </div>
    );
};

export default Todo;